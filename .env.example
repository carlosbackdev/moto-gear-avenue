# Backend API Configuration
# URL base del backend Spring Boot (incluye /api)
VITE_API_BASE_URL=http://localhost:8080/api

# Image Base URL Configuration
# URL base para las imágenes (sin /api)
# Las rutas de imagen del backend se concatenarán con esta URL
# Ejemplo: VITE_IMAGE_BASE_URL + /uploads/products/image.webp
VITE_IMAGE_BASE_URL=http://localhost:3001

# Google OAuth Configuration
# Client ID de Google Cloud Console para autenticación
# Obtén uno en: https://console.cloud.google.com/apis/credentials
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com